package com.uzproc.backend.repository;

import com.uzproc.backend.entity.ApprovalPresentation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApprovalPresentationRepository extends JpaRepository<ApprovalPresentation, Long> {
}
